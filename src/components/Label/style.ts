import { styled } from '../../../stitches.config';

export const Root = styled('label', {
  display: 'inline-block',
  fontSize: '15px',
  fontWeight: '500',
  marginBottom: '8px',
  color: '$gray11',
});
